export const getGlobalData = () => {
  const name = 'VenusHui';
  const blogTitle = 'VenusHui\'s Blog';
  const footerText = '心有所向，日复一日，必有精进';

  return {
    name,
    blogTitle,
    footerText,
  };
};
